import {ImageResponse} from "next/og";import {getSiteContent,optional} from "./data";
export const size={width:1200,height:630};export const contentType="image/png";
export const dynamic="force-dynamic";
export default async function Image(){const {data:copy}=await optional(()=>getSiteContent("open_graph"),{});return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",padding:"72px",background:copy.background,color:copy.foreground,fontFamily:"sans-serif",border:`2px solid ${copy.border}`}}><div style={{display:"flex",color:copy.accent,fontSize:22,letterSpacing:5}}>{copy.eyebrow}</div><div style={{display:"flex",flexDirection:"column"}}><span style={{fontSize:78,fontWeight:700,letterSpacing:-4}}>{copy.title}</span><span style={{fontSize:30,color:copy.muted,marginTop:20}}>{copy.subtitle}</span></div><div style={{display:"flex",justifyContent:"space-between",fontSize:20,color:copy.muted}}><span>{copy.categories}</span><span style={{color:copy.accent}}>{copy.status}</span></div></div>,size)}
